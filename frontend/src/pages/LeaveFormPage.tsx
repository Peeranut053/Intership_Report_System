// This fill-in-the-form flow for the ใบลา was replaced by a plain "download
// a blank PDF template" action (see StudentDocumentsPage.tsx and
// api.ts's downloadLeaveFormPdf) — the student asked for an unfilled,
// print-and-hand-sign template rather than one pre-filled from their
// profile data. Kept as an empty module (rather than deleted, which this
// environment doesn't support) so nothing importing the old path breaks.
export {};
